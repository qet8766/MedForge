DOMAIN=example.com
ACME_EMAIL=admin@example.com
CF_API_TOKEN=

DB_ROOT_PASSWORD=
DB_USER=medforge
DB_PASSWORD=

SESSION_SECRET=

# Pack image reference (digest-pinned).
# Build with:  docker build -t medforge-pack-default infra/packs/default
PACK_IMAGE=medforge-pack-default:latest
